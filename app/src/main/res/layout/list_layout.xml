<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/search_bar_in_memo_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_margin="@dimen/default_margin"
        android:inputType="text"
        android:textSize="@dimen/text_size_15sp" />

    <Button
        android:id="@+id/btn_add_in_list"
        android:layout_alignParentStart="true"
        android:layout_below="@id/search_bar_in_memo_list"
        android:layout_margin="@dimen/default_margin"
        android:textSize="@dimen/text_size_15sp"
        android:text="@string/create_new"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ScrollView
        android:id="@+id/memo_list_scroll"
        android:layout_below="@id/btn_add_in_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:gravity="top"
            android:orientation="vertical"
            android:layout_margin="@dimen/default_margin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/element_in_list"
                android:text="メモ1"
                android:textSize="@dimen/top_text_size"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </LinearLayout>
    </ScrollView>
</RelativeLayout>